{
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "scripts": {
    "dev:backend": "cd backend && nodemon",
    "dev:frontend": "cd frontend && npm run dev",
    "all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  }
}